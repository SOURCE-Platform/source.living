import type { TranscriptData, ChapterSummary } from "@/components/audio-player/context/types";

export const TRANSCRIPT_DATA: TranscriptData = {
    utterances: []
};

export const CHAPTERS_DATA: ChapterSummary[] = [];
